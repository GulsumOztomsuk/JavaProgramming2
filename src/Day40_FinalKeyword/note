fields==by the instance variables
Encapculation: data hiding
               give private acces modofiers to fields
               generate public getter/setter to read/write the private fields

using thge data:
		reading the data
		writing the data


Inheritance: Is A relation.
			 parent & child classes ( super & sub)

			class A  extends Class B

			child class (sub): inherits the variables and methods (Visible to the child class) from parent class (super)

			parent class (super): can not inherit anything from child (sub class)



protected access modifier: Alayws visible within the same package
						   Visible outside the package in subclass ONLY
Final Keyword: Unchangable
